{
  "komari_url": {
    "type": "string",
    "description": "Komari 服务器地址",
    "hint": "您的 Komari 监控面板地址 (例如 https://status.example.com)",
    "default": ""
  },
  "komari_token": {
    "type": "string",
    "description": "API Token",
    "hint": "API Key 或 Session Token (如果是非公开站点则必须)",
    "default": ""
  },
  "verify_ssl": {
    "type": "bool",
    "description": "校验 TLS 证书",
    "hint": "开启时校验服务器证书（默认安全）。仅在使用自签名证书且确认网络可信时关闭，关闭有中间人风险。",
    "default": true
  },
  "image_output": {
    "type": "bool",
    "description": "以图片形式发送",
    "hint": "开启后，状态报告将调用文本转图像服务以图片形式发送。",
    "default": false
  },
  "dark_theme": {
    "type": "bool",
    "description": "是否启用深色背景",
    "hint": "开启后，生成的图片将使用深色主题背景。",
    "default": true
  },
  "viewport_width": {
    "type": "int",
    "description": "图片宽度",
    "hint": "生成图片的宽度（像素），高度会自动适应内容",
    "default": 600,
    "minimum": 300,
    "maximum": 2000
  },
  "trigger_nodes": {
    "type": "string",
    "description": "节点状态指令(正则)",
    "hint": "自定义查询节点状态的触发指令，支持正则表达式",
    "default": "查询\\s*Komari\\s*节点状态"
  },
  "trigger_realtime": {
    "type": "string",
    "description": "实时状态指令(正则)",
    "hint": "自定义查询实时状态的触发指令，支持正则表达式",
    "default": "查询\\s*Komari\\s*实时状态"
  },
  "trigger_public": {
    "type": "string",
    "description": "公开设置指令(正则)",
    "hint": "自定义查询公开设置的触发指令，支持正则表达式",
    "default": "查询\\s*Komari\\s*公开设置"
  },
  "trigger_version": {
    "type": "string",
    "description": "版本信息指令(正则)",
    "hint": "自定义查询版本信息的触发指令，支持正则表达式",
    "default": "查询\\s*Komari\\s*版本信息"
  }
}
